name: Run IPA Builder

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build IPA
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Confirm manual workflow works
        shell: bash
        run: |
          echo "Run IPA Builder button works."
          echo "Next step is adding the IPA build script back safely."
